# Prompt Log — Лабораторная работа №14, Вариант 19

## Задание: Анализ энергопотребления 

### Промпт 1

**Инструмент:** SourceCraft

**Промпт:**
```
Разработай подробный план архитектуры для сервиса анализа энергопотребления.
Источник данных: Счётчики электроэнергии (эмуляция) 
Требования:
1. Распределённый сборщик на Go (координация через etcd) 
Реализовать систему, где несколько экземпляров Go-сборщика могут работать 
параллельно. Использовать etcd для координации и распределения шардов/источников. 
2. Оконная агрегация в Go перед отправкой в Python 
Добавить в сборщик оконную агрегацию (tumbling window). Например: каждые N 
секунд или каждые M записей агрегировать данные на стороне Go и отправлять в Python не
исходные записи, а уже агрегированные (суммы, средние и т.д.).
3. Передача данных через Apache Arrow 
Заменить передачу через JSON-файлы на передачу через Apache Arrow (Flight RPC 
или RecordBatch). Реализовать Go-сервер, отдающий данные в формате Arrow, и Python
клиент, принимающий их.
4. Интеграция Rust-библиотеки для валидации 
Написать на Rust библиотеку для валидации данных. Встроить её в Go-сборщик через cgo или в Python-анализатор через PyO3. 
5. Развёртывание в Kubernetes с автоскалированием 
Упаковать Go-сборщик в Docker-образ. Развернуть конвейер в minikube/k3s. 
Настроить HPA для сборщика на основе длины очереди или загрузки CPU. 
Сравнение производительности: Go vs Python для сбора 
Реализовать сборщик данных на Python (с использованием asyncio/aiohttp). 
Сравнить производительность между Go- и Python-версиями при одинаковой нагрузке.
Обработка потоковых данных (Kafka) 
Сделать потоковую передачу через Kafka (или NATS). Go-сборщик пишет в топик, Python-анализатор читает из топика и обрабатывает в реальном времени. Добавить оконную обработку. 
6. Веб-дашборд с обновлением в реальном времени 
Разработать веб-дашборд на Streamlit (или FastAPI + WebSocket), который 
показывает текущую агрегированную статистику и графики, обновляющиеся в реальном 
времени по мере поступления новых данных от сборщика. 
Опиши структуру папок.
```

**Результат:** Получен детальный план со структурой каталогов

### Промпт 2

**Инструмент:** SourceCraft

**Промпт:**
```
Docker не собирается, перепроверь все и исправь ошибки
```

**Результат:** Исправлены Dockerfil и docker-compose

### Промпт 3

**Инструмент:** SourceCraft

**Промпт:**
```
При сборке возникают проблемы с зависимостями go и rust
```

**Результат:** Исправлены зависимости

### Промпт 4

**Инструмент:** SourceCraft

**Промпт:**
```
При переходе http://localhost:8501/:
Не удалось подключиться к базе данных. Проверьте настройки подключения.
```

**Результат:** Исправлены web‑dashboard и python‑processor, исправлено подключение к БД

### Промпт 5

**Инструмент:** SourceCraft

**Промпт:**
```
Надо реализовать GO продюссера для отправки данных в Kafka. Реализацуй оконную обработку на стороне Kafka Streams. Реализуй Python сборщика для сравнения с GO. Реализуй отправку данных из сборщика (сейчас заглушка sendViaArrowFlight)
```

**Результат:** Исправлены указанные части


### Итого

- Количество промптов: 5
- Что пришлось исправлять вручную: ничего
- Время: ~3 часа

---